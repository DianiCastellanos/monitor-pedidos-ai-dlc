# Copy this file to .env and fill in your local values.
# .env is gitignored — never commit it.

# PostgreSQL connection (overrides appsettings.json)
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=MonitorPedidosDb;Username=YOUR_DB_USER;Password=YOUR_DB_PASSWORD
